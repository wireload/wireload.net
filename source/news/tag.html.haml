---
pageable: true
per_page: 10
---

%h1.header
  Articles tagged
  = '"' + tagname + '"'

%ul#news-list
  - page_articles.each do |current_article|
    = partial :blog_article_list_format, :locals => { :current_article => current_article }

= partial :blog_pagination, :locals => { :prev_page => prev_page, :page_number => page_number, :num_pages => num_pages, :next_page => next_page }

%script{:async => "", :src => "//disqus.com/forums/wireload/count.js", :type => "text/javascript"}
